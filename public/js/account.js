// function buildUpdateAccount(data) { 
//     let accountDisplay = document.getElementById("accountDisplay"); 
//     // Iterate over all vehicles in the array and put each in a row 
//     data.forEach(function (element) { 
//      console.log(element.inv_id + ", " + element.inv_model); 
//      accountLink += `<a href="/account/update/${element.account_id}'> Update Account</a>`
//     }) 
//     // Display the contents in the Account Management view 
//     accountDisplay.innerHTML = accountLink; 
//    }